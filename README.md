#Notas:

Este es mi primer programa en Node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la table de multiplicar      [número] [requerido]
  -l, --listar   Muestra el resultado de tabla de multiplicacion
                                                     [booleano] [defecto: false]
  -h, --hasta    Especifica hasta que numero se va a multiplicar la base        
                                                          [número] [defecto: 10]
```